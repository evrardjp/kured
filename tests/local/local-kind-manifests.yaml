apiVersion: v1
kind: Service
metadata:
  name: n-r-d
  namespace: kube-system
  labels:
    app: node-reboot-detector
spec:
  type: NodePort
  selector:
    app: node-reboot-detector
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080